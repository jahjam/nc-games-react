import * as Styled from './styles';

const WrongPath = () => {
  return (
    <Styled.WrongPath>
      <span>Sorry this page doesn't exist!</span>
    </Styled.WrongPath>
  );
};

export default WrongPath;
